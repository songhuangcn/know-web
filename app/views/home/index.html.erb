<% provide(:title, '') %>

<!-- 轮播图 -->
<%= render 'banner' %>

<!-- 技能大纲 -->
<%= render 'outline' %>

<!-- 项目演示 -->
<%= render 'project' %>

<!-- 名博推荐 -->
<%= render 'blogger' %>

<!-- 关于 -->
<%= render 'about' %>

<script type="text/javascript">
	// 轮播
	var interval;
	slide(interval, 5000);

	// 项目演示动画
	changeDemo();

	// 离开页面停止计时器
	leaveStop();
</script>
